import Link from'next/link';import{teachers}from'../lib/teachers';
export default function HomePage(){return <main className="shell"><section className="hero"><div className="badge">BÁO GIẢNG ĐIỆN TỬ</div><h1>Cổng giáo viên</h1><p>Một hệ thống dùng chung. Mỗi giáo viên có đường dẫn riêng và được tự động nhận diện khi mở app.</p></section><section className="panel"><div className="panelHead"><div><h2>Danh sách giáo viên</h2><p>{teachers.length} đường dẫn đã cấu hình</p></div><Link className="adminLink" href="/admin">Quản trị cấu hình</Link></div><div className="grid">{teachers.map(t=><Link className="teacher" href={`/gv/${t.slug}`} key={t.slug}><span className="avatar">{t.fullName.split(' ').slice(-1)[0].slice(0,1)}</span><span className="teacherText"><b>{t.fullName}</b><small>{t.subject}{t.team?` · ${t.team}`:''}</small></span><span className="go">→</span></Link>)}</div></section></main>}
