import Link from'next/link';import{teachers}from'../../lib/teachers';
export default function AdminPage(){const backend=Boolean(process.env.GAS_BACKEND_URL);return <main className="shell"><section className="hero compact"><div className="badge">QUẢN TRỊ</div><h1>Cấu hình hệ thống</h1><p>Kiểm tra danh mục giáo viên và kết nối Google Apps Script.</p></section><section className="panel"><div className={`status ${backend?'ok':'warn'}`}><b>{backend?'✓ Backend đã cấu hình':'⚠ Chưa cấu hình GAS_BACKEND_URL'}</b><span>{backend?'Vercel đã có địa chỉ API Apps Script.':'Sau khi deploy Apps Script Web App, thêm URL /exec vào Environment Variables trên Vercel.'}</span></div><div className="panelHead"><div><h2>Giáo viên</h2><p>{teachers.length} cấu hình</p></div><Link className="adminLink" href="/">← Cổng giáo viên</Link></div><div className="tableWrap"><table><thead><tr><th>Giáo viên</th><th>Mã</th><th>Môn</th><th>Tổ</th><th>Đường dẫn</th></tr></thead><tbody>{teachers.map(t=><tr key={t.slug}><td><b>{t.fullName}</b></td><td>{t.key}</td><td>{t.subject}</td><td>{t.team||'—'}</td><td><Link href={`/gv/${t.slug}`}>/gv/{t.slug}</Link></td></tr>)}</tbody></table></div></section></main>}
